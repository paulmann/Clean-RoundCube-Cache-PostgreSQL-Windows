# Clean-RoundCube-Cache-PostgreSQL-Windows
PowerShell 7.5 and BAT scripts to truncate all Roundcube cache tables in PostgreSQL on Windows. Auto-detects psql.exe via PATH, env vars, registry and filesystem scan.
